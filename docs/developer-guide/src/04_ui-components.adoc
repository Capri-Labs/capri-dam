[[section-ui-components]]
= UI Components

The React frontend is organized into domain folders under
`app/javascript/components/`. Complex modules are mounted as islands inside the
Rails shell and typically fetch JSON from `/api/v1/...`.

== Core library screens

[cols="1,3", options="header"]
|===
| Component area | Notes
| `Folders/*` | Asset explorer, folder tree, upload flows, and the image editor dialog.
| `Search/*` | Full-text and semantic search experiences layered on asset APIs.
| `Dashboard/*` | Top-level KPI and activity screens.
| `Bin/*` | Dedicated recycle-bin manager with stats, filtering, and mixed item restore/delete.
| `Collections/*` | Workspace board, detail view, rule simulation, and semantic cluster map.
| `Duplicates/*` | Duplicate-review dashboard with modal resolution workflow.
| `NotificationBell.jsx` | Global unread bell polling `/api/v1/notifications`.
|===

== Admin surfaces

[cols="1,3", options="header"]
|===
| Component | Responsibility
| `Admin/UsersManager.jsx`, `UserDrawer.jsx` | User CRUD, role changes, membership, and account detail.
| `Admin/UserGroupsManager.jsx` | Hierarchy management for nested groups.
| `Admin/AclMatrix.jsx` | Folder-policy matrix editing against the current `read_access`/`modify_access` style columns.
| `Admin/EmailEngineManager.jsx` | Template editing, event mapping, and outbox monitoring.
| `Admin/SystemStatus/index.jsx` | Operational status tabs for AI gateway, SMTP, observability, feature flags, and storage operations.
| `Admin/Reports/AnalyticsDashboard.jsx` | Analytics charts and export launch point.
| `Admin/Migrations/*` | Connectors, ingestion dashboard, data-health review, and pre-flight tooling.
| `Admin/Intelligence/*` | Model hub, provenance policy, batch AI jobs, prompt playground, semantic copilot, and agent workflows.
|===

== Tools and settings screens

[cols="1,3", options="header"]
|===
| Area | Notes
| `Tools/MetadataSchemas/*` | Hierarchical schema tree, folder assignment, and tab/field editing.
| `Tools/MetadataExport/*` | Export creation, property discovery, scheduling, and download UI.
| `Tools/MetadataImport/*` | CSV import upload, column mapping, delimiter controls, and polling.
| `Tools/AssetConfigurations/*` | Houses duplicate-manager settings and related operational asset policies.
| `Profile/*` and `Settings/*` | User preferences and personal account flows.
|===

== Workflow and creative tooling

[cols="1,3", options="header"]
|===
| Component | Notes
| `Workflows/WorkflowDesigner.jsx` | Top-level designer shell and persistence orchestration.
| `Workflows/WorkflowCanvas.jsx` | React Flow canvas with node-to-backend type mapping.
| `Workflows/NodePalette.jsx` | User-facing catalogue of workflow node types.
| `Workflows/ApprovalActions.jsx` | Runtime decision controls for assigned tasks.
| `Workflows/BottleneckReport.jsx` | SLA and queue analytics.
| `Workflows/BulkReassignModal.jsx` | Mass assignment/rebalancing tool.
| `Folders/ImageEditorDialog.jsx` | Multi-tab image editor: Suggestions, Adjust, AI Studio, LUT filters, geometry, and custom CLI.
|===

== Global layout (Header & Footer)

[cols="1,3", options="header"]
|===
| Component | Notes
| `Layout/Header.jsx` | Fixed top `AppBar` — product logo (`/images/logo.png`), global search bar, quick-create menu, notification bell, and the profile/impersonation menu.
| `Layout/Footer.jsx` | Global site footer, redesigned to match the product logo/brand gradient (indigo `#4f46e5` \-> violet `#7c3aed`, shown as a thin accent stripe across the top of the footer). Renders the product logo (scaled down), a tagline, and three link columns: *Product* (`/dashboard`, `/search`, `/workflows`, `/duplicates`), *Resources* (`/api/rest`, `/api/graphql`, `/api-docs`), and *Legal & Security* (`/privacy`, `/terms`, `/compliance`). The bottom bar shows copyright, an "All systems operational" status dot, and the build version. Fully i18n'd via the `footer.*` translation namespace in `app/javascript/i18n/locales/*.json` — no literal English strings are hard-coded in JSX.
| `Layout/ImpersonationBanner.jsx` | Warning banner rendered above the `AppBar` while an admin is impersonating another user.
|===

== Upload & Enrich overlay

`Folders/UploadWorkspace.jsx` is the full-screen upload dialog launched from the
"Upload Asset" button in `ExplorerTopBar.jsx`. It composes `UploadSidebar.jsx`
(global assignment/tags/AI actions) and `UploadGrid.jsx` (the per-file staging
grid), and drives duplicate detection via `DuplicateResolverDialog.jsx`.

[cols="1,3", options="header"]
|===
| Behaviour | Notes
| Tag merging on upload | `handleUploadAll()` sends a single de-duplicated `tags` metadata array that merges the sidebar's global `manualTags` (typed into the Tags `Autocomplete`, `data-testid="upload-manual-tags"`) with each file's own `meta.aiTags` (set per-file by the "Run AI enhance on this file" button in `UploadGrid.jsx`). Both sources previously landed in different, disconnected pieces of state — only the sidebar's manual tags were actually sent to `POST /api/v1/assets`, so AI tags shown in the staging grid never made it into `Asset#properties.tags` and never appeared in `AssetTagsEditor.jsx`/`AssetViewer.jsx` afterward. `data-testid="upload-global-asset-type"` marks the "Select Global asset type" `Select` for e2e targeting.
| Duplicate detection at upload time | Dropped files are hashed client-side (`calculateFileHash`) and checked in a single batch via `POST /api/v1/assets/check_hashes`. A match sets `isDuplicate: true` on that file and shows an amber "Duplicate Found" chip in `UploadGrid.jsx`, opening `DuplicateResolverDialog.jsx`'s `LegacyDuplicateResolverDialog` for side-by-side comparison and resolution (Skip / Upload anyway / AI merge for identical bytes).
| Bin-aware duplicate matches | `check_hashes` returns `in_bin: asset.deleted_at.present?` per matched duplicate (see <<section-duplicate-detection>>). `LegacyDuplicateResolverDialog` renders the same `BinChip` pattern used by Search results (`SearchResultCard.jsx`) as an overlay badge on the primary comparison image, and next to any per-location chip, whenever the matched existing asset currently lives in the Recycle Bin — so a user can't be confused into thinking a soft-deleted asset is still active in the library.
|===

NOTE: New module-specific details are documented in <<section-admin-module>>,
<<section-collections-module>>, <<section-bin-module>>, <<section-duplicate-detection>>,
<<section-intelligence-module>>, <<section-ingestion-migration>>, and
<<section-image-editor>>.
