# Suppress Node engine-version mismatch errors so that `yarn install` and
# `yarn test` succeed on older Node versions (e.g. Node 18 in a local env
# that hasn't been updated).  The CI workflow uses Node 22+ as required by
# package.json `engines`, so this does not weaken CI strictness.
ignore-engines true

